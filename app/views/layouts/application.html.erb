<!DOCTYPE html>
<html data-theme="corporate" lang="it">
  <head>
    <title><%= content_for(:title) || "Piertutor" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= render 'application/favicon' %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Cloudflare Web Analytics --><script defer src='https://static.cloudflareinsights.com/beacon.min.js' data-cf-beacon='{"token": "67d108b845b045c39e0520f65d6c3c49"}'></script><!-- End Cloudflare Web Analytics -->
  </head>

  <body class="bg-black">
    <div>
      <% if flash[:success] %>
        <div class="toast toast-bottom toast-center min-w-64 md:min-w-128 z-10">
          <div class="alert alert-success flex justify-center items-center">
            <span><%= flash[:success] %></span>
          </div>
        </div>
      <% end %>
      <% if flash[:notice] %>
        <div class="toast toast-bottom toast-center min-w-64 md:min-w-128 z-10">
          <div class="alert alert-warning flex justify-center items-center">
            <span><%= flash[:notice] %></span>
          </div>
        </div>
      <% end %>
      <% if flash[:error] %>
        <div class="toast toast-bottom toast-center min-w-64 md:min-w-128 z-10">
          <div class="alert alert-error flex justify-center items-center">
            <p><%= flash[:error] %></p>
          </div>
        </div>
      <% end %>
    </div>
    <main class="bg-white">
      <nav class="top-0 left-0 right-0 z-50 bg-black border-b border-primary" data-controller="menu" aria-label="Main navigation">
        <div class="max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div class="flex justify-between items-center">
            <!-- Logo -->
            <a href="/" class="text-2xl font-bold text-white hover:text-primary active:text-primary transition-colors" aria-label="PierTutor Home">@piertutor</a>

            <!-- Desktop Menu -->
            <div class="hidden md:flex space-x-6 items-center">
              <a href="/contatti" class="text-lg text-white hover:text-primary active:text-primary transition-colors">Contatti</a>
              <a href="/affiliati" class="text-lg text-white hover:text-primary active:text-primary transition-colors">Prodotti Affiliati</a>
              <% if current_user %>
                <div class="ml-8">
                  <%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete } ,class: "btn btn-outline btn-error w-full" %>
                </div>
              <% end %>
            </div>

            <!-- Mobile Menu Toggle -->
            <button 
              class="md:hidden text-white focus:outline-none relative w-6 h-6" 
              aria-label="Toggle navigation menu"
              data-action="click->menu#toggle"
            >
              <%= inline_svg_tag "open-menu.svg", class: "w-6 h-6 fill-white stroke-white cursor-pointer absolute top-0 left-0 transition-opacity duration-300", data: { menu_target: "openIcon" } %>
              <%= inline_svg_tag "close-menu.svg", class: "w-6 h-6 fill-white stroke-white cursor-pointer absolute top-0 left-0 transition-opacity duration-300 hidden", data: { menu_target: "closeIcon" } %>
            </button>
          </div>

          <!-- Mobile Menu -->
          <div 
            class="md:hidden hidden" 
            data-menu-target="menu"
          >
            <div class="flex flex-col items-end pt-8 space-y-2">
              <a href="/contatti" class="text-xl text-white hover:text-primary active:text-primary transition-colors">Contatti</a>
              <a href="/affiliati" class="text-xl text-white hover:text-primary active:text-primary transition-colors">Prodotti Affiliati</a>
              <% if current_user %>
                <div class="mt-8">
                  <%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete } ,class: "btn btn-outline btn-error w-full" %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </nav>
      <%= yield %>
    </main>
    <%# FOOTER %>
    <footer class="w-full bg-base-200 text-base-content py-16 px-4 sm:px-8">
      <div class="max-w-screen-xl mx-auto flex flex-col md:flex-row md:justify-between gap-12">

        <!-- Logo -->
        <div class="flex flex-col items-center md:items-start gap-4 text-center md:text-left">
          <h1 class="text-4xl font-extrabold tracking-tight">@piertutor</h1>
          <p class="text-sm text-base-content/70">
            Camionista • Content Creator • Volvo Trucks Ambassador
          </p>
        </div>

        <!-- Social Links -->
        <div class="flex flex-col items-center md:items-start gap-2 text-center md:text-left">
          <h3 class="text-lg font-bold mb-2">Seguimi</h3>
          <div class="flex flex-col md:flex-row flex-wrap justify-center md:justify-start gap-4">
            <a href="https://www.instagram.com/piertutor" class="hover:text-primary transition">Instagram</a>
            <a href="https://www.facebook.com/gianni.bugno.1" class="hover:text-primary transition">Facebook</a>
            <a href="https://www.tiktok.com/@piertutor" class="hover:text-primary transition">TikTok</a>
            <a href="https://www.youtube.com/c/PierantonioDePiccoli" class="hover:text-primary transition">YouTube</a>
          </div>
        </div>
      </div>

      <!-- Bottom Bar -->
      <div class="mt-12 border-t border-base-300 pt-6 text-center text-sm text-base-content/70">
        © <%= Time.now.year %> Piertutor. Tutti i diritti riservati.
      </div>
    </footer>
  </body>
</html>
