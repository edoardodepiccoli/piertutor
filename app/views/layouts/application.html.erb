<!DOCTYPE html>
<html lang="it" class="scroll-pt-24">
  <head>
    <title><%= content_for(:title) || "Piertutor" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= render 'application/favicon' %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Cloudflare Web Analytics -->
    <script defer src='https://static.cloudflareinsights.com/beacon.min.js' data-cf-beacon='{"token": "67d108b845b045c39e0520f65d6c3c49"}'></script>
    <!-- End Cloudflare Web Analytics -->

    <!-- Google Fonts -->
    <link rel="preload" href="https://fonts.googleapis.com">
    <link rel="preload" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Archivo+Black&family=Archivo:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <!-- End Google Fonts -->
  </head>

  <body class="bg-black">
    <!-- Navbar -->
    <nav class="fixed top-0 left-0 right-0 z-50" data-controller="menu">
      <div class="bg-black/90 backdrop-blur border-b-2 border-brand shadow-lg">
        <div class="max-w-[1200px] mx-auto px-8 py-4 flex items-center justify-between lg:px-0">
          <%= link_to "@piertutor", root_path, class: "text-white archivo text-xl lg:text-2xl hover:text-brand transition-colors" %>

          <!-- Desktop links -->
          <div class="hidden lg:flex items-center gap-8">
            <%= link_to "Contatti", root_path(anchor: "contact"), class: "text-white/90 hover:text-brand transition-colors font-semibold" %>
            <%= link_to "Prodotti affiliati", affiliati_path, class: "translate-x-[2px] inline-flex items-center px-4 py-2 bg-brand text-black border border-black shadow-[-3px_3px_0_0_#FFF] transition-all duration-150 hover:translate-x-[-3px] hover:translate-y-[3px] hover:shadow-none active:translate-x-[-3px] active:translate-y-[3px] active:shadow-none text-sm lg:text-base font-semibold" %>
          </div>

          <!-- Mobile menu button -->
          <button
            type="button"
            class="lg:hidden inline-flex items-center justify-center w-11 h-11 border border-white/20 text-white hover:text-brand hover:border-brand transition-colors"
            aria-label="Apri menu"
            data-action="menu#toggle"
          >
            <svg data-menu-target="openIcon" class="w-6 h-6" viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path d="M4 6h16M4 12h16M4 18h16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
            <svg data-menu-target="closeIcon" class="w-6 h-6 hidden" viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path d="M4 4l16 16M20 4L4 20" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile menu overlay -->
      <div
        data-menu-target="backdrop"
        data-action="click->menu#close"
        class="fixed inset-0 z-40 bg-black/60 opacity-0 pointer-events-none transition-opacity duration-200"
        aria-hidden="true"
      ></div>

      <div
        data-menu-target="panel"
        class="fixed top-0 right-0 z-50 h-[100dvh] w-[85vw] max-w-sm bg-black border-l-2 border-brand shadow-2xl translate-x-full transition-transform duration-300 will-change-transform"
        role="dialog"
        aria-modal="true"
        aria-label="Menu"
      >
        <div class="px-6 pt-6 pb-8 flex flex-col gap-6">
          <div class="flex items-center justify-between">
            <%= link_to "@piertutor", root_path, class: "text-white archivo text-xl hover:text-brand transition-colors", data: { action: "click->menu#close" } %>
            <button
              type="button"
              class="inline-flex items-center justify-center w-11 h-11 border border-white/20 text-white hover:text-brand hover:border-brand transition-colors"
              aria-label="Chiudi menu"
              data-action="menu#close"
            >
              <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <path d="M4 4l16 16M20 4L4 20" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
              </svg>
            </button>
          </div>

          <div class="flex flex-col gap-3">
            <%= link_to "Contatti", root_path(anchor: "contact"), class: "w-full px-4 py-3 border border-white/15 text-white/90 hover:text-brand hover:border-brand transition-colors font-semibold", data: { action: "click->menu#close" } %>
            <%= link_to "Prodotti affiliati", affiliati_path, class: "w-full px-4 py-3 bg-brand text-black border border-black shadow-[-3px_3px_0_0_#FFF] transition-all duration-150 hover:translate-x-[-3px] hover:translate-y-[3px] hover:shadow-none active:translate-x-[-3px] active:translate-y-[3px] active:shadow-none font-semibold", data: { action: "click->menu#close" } %>
          </div>
        </div>
      </div>
    </nav>

    <div id="flash_messages" class="fixed bottom-6 inset-x-0 flex flex-col items-center space-y-3 z-50">
      <% flash.each do |type, message| %>
        <% next if message.blank? %>

        <% icon_and_accent =
          case type.to_sym
          when :success
            { icon: '<path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>',
              border: "border-green-400", bg: "bg-black" }
          when :error
            { icon: '<path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0zm-9 3.75h.008v.008H12v-.008z"/>',
              border: "border-red-400", bg: "bg-black" }
          else
            { icon: '<path stroke-linecap="round" stroke-linejoin="round" d="M11.25 11.25l.041-.02a.75.75 0 011.063.852l-.708 2.836a.75.75 0 001.063.853l.041-.021M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-9-3.75h.008v.008H12V8.25z"/>',
              border: "border-brand", bg: "bg-black" }
          end
        %>

        <div
          data-controller="flash"
          data-flash-duration-value="3000"
          class="flex items-center gap-3 px-5 py-3 border-2 <%= icon_and_accent[:border] %> <%= icon_and_accent[:bg] %> text-white
                 shadow-[0_4px_20px_rgba(0,0,0,0.4)] transform transition-all duration-500 ease-in-out
                 translate-y-full opacity-0"
        >
          <svg class="w-5 h-5 shrink-0" style="color: #FFFF04;" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <%= icon_and_accent[:icon].html_safe %>
          </svg>
          <span class="text-sm font-semibold tracking-wide"><%= message %></span>
        </div>
      <% end %>
    </div>
    <main>
      <%= yield %>
    </main>
    <%# FOOTER %>
    <footer class="relative bg-white text-black border-t border-gray-200">
      <div class="max-w-[1200px] mx-auto px-6 py-12 lg:px-0 lg:py-20 grid gap-12 lg:grid-cols-3 lg:gap-16">

        <!-- Logo & Claim -->
        <div class="text-center lg:text-left">
          <h3 class="text-4xl lg:text-6xl archivo-black mb-4">@piertutor</h3>
          <p class="text-gray-600 max-w-sm mx-auto lg:mx-0 lg:text-base">
            Camionista, Content Creator, Volvo Trucks Ambassador
          </p>
        </div>

        <!-- Social Links -->
        <div class="text-center">
          <h3 class="text-lg font-bold mb-4 uppercase tracking-wide">Seguimi</h3>
          <div class="flex flex-wrap justify-center gap-4">
            <a href="https://www.instagram.com/piertutor" class="hover:text-primary font-medium transition-colors">Instagram</a>
            <a href="https://www.facebook.com/gianni.bugno.1" class="hover:text-primary font-medium transition-colors">Facebook</a>
            <a href="https://www.tiktok.com/@piertutor" class="hover:text-primary font-medium transition-colors">TikTok</a>
            <a href="https://www.youtube.com/c/PierantonioDePiccoli" class="hover:text-primary font-medium transition-colors">YouTube</a>
          </div>
        </div>

        <!-- Bottom Right -->
        <div class="flex flex-col items-center justify-start text-center lg:items-end lg:text-right gap-4">
          <p class="text-sm text-gray-500">© 2025 Piertutor. Tutti i diritti riservati.</p>
          <% if current_user %>
            <%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete } %>
          <% end %>
        </div>
      </div>
    </footer>
    <div class="h-16 bg-gray-100 text-black flex justify-center items-center">
      <p class="text-sm">Sito web realizzato da Edoardo De Piccoli</p>
    </div>
  </body>
</html>